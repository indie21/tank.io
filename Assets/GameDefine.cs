using UnityEngine;
using System.Collections;

public class GameDefine : MonoBehaviour {

	public static Vector2 matrix = new Vector2(100,100);

	// Use this for initialization
	void Start () {
		Debug.Log (matrix.x);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
