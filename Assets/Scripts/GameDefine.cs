using UnityEngine;

public class GameDefine : MonoBehaviour {

    public static Vector2 _matrix = new Vector2(100,100);

    // Use this for initialization
    void Start () {
        Debug.Log (_matrix.x);
    }

    // Update is called once per frame
    void Update () {
    }
}
